import template from './lesson-list.html';
import controller from './lesson-list.controller';

let lessonListComponent = {
  bindings: {},
  template,
  controller
};

export default lessonListComponent;
