import template from './strengthFormula.html';
import controller from './strengthFormula.controller';

let strengthFormulaComponent = {
  bindings: {},
  template,
  controller
};

export default strengthFormulaComponent;
